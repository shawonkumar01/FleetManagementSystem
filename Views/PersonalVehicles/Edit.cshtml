@model PersonalVehicle
@{
    ViewData["Title"] = "Edit Vehicle";
    Layout = ViewData["Layout"]?.ToString() ?? "~/Views/Shared/_Layout.cshtml";
}

<div class="panel" style="max-width:600px;">
    <div class="panel-header">
        <h5><i class="fas fa-edit"></i> Edit Vehicle</h5>
    </div>
    <div class="panel-body">
        <form asp-action="Edit" method="post">
            <input type="hidden" asp-for="Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:16px;"></div>

            <div class="form-group">
                <label class="form-label">Vehicle Name</label>
                <input asp-for="Name" class="form-input" required />
            </div>

            <div class="grid-2" style="display:grid; grid-template-columns:1fr 1fr; gap:16px;">
                <div class="form-group">
                    <label class="form-label">Make (Brand)</label>
                    <input asp-for="Make" class="form-input" required />
                </div>
                <div class="form-group">
                    <label class="form-label">Model</label>
                    <input asp-for="Model" class="form-input" required />
                </div>
            </div>

            <div class="grid-3" style="display:grid; grid-template-columns:repeat(3, 1fr); gap:16px;">
                <div class="form-group">
                    <label class="form-label">Year</label>
                    <input asp-for="Year" class="form-input" type="number" required />
                </div>
                <div class="form-group">
                    <label class="form-label">License Plate</label>
                    <input asp-for="LicensePlate" class="form-input" />
                </div>
                <div class="form-group">
                    <label class="form-label">Odometer (km)</label>
                    <input asp-for="CurrentOdometer" class="form-input" type="number" />
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">VIN</label>
                <input asp-for="VIN" class="form-input" maxlength="17" />
            </div>

            <div class="grid-2" style="display:grid; grid-template-columns:1fr 1fr; gap:16px;">
                <div class="form-group">
                    <label class="form-label">Purchase Date</label>
                    <input asp-for="PurchaseDate" class="form-input" type="date" />
                </div>
                <div class="form-group">
                    <label class="form-label">Purchase Price</label>
                    <input asp-for="PurchasePrice" class="form-input" type="number" step="0.01" />
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">Notes</label>
                <textarea asp-for="Notes" class="form-input" rows="3"></textarea>
            </div>

            <div style="display:flex; gap:12px; margin-top:24px;">
                <button type="submit" class="btn-indigo">
                    <i class="fas fa-save"></i> Update Vehicle
                </button>
                <a asp-action="Index" class="btn-ghost">Cancel</a>
            </div>
        </form>
    </div>
</div>

<style>
    .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
    @@media (max-width: 640px) {
        .grid-2, .grid-3 { grid-template-columns: 1fr; }
    }
</style>
